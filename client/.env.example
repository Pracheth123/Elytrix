# Optional. Leave empty in development (Vite proxies /api to the server).
# In production, set this if the API is hosted on a different origin,
# e.g. VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
